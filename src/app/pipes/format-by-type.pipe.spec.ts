import { FormatByTypePipe } from './format-by-type.pipe';

describe('FormatByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
