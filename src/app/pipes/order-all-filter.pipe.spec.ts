import { OrderAllFilterPipe } from './order-all-filter.pipe';

describe('OrderAllFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAllFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
